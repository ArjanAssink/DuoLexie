node_modules/
dist/
*.tsbuildinfo
*.local
.DS_Store
.env
.env.*
test-results/
playwright-report/
# the voice recordings never go in the repo — docs/private-audio.md. Local clips may sit
# here for dev; nothing under it is committed or deployed
app/public/audio/
recordings/clips/
*.mp3
*.wav
*.webm
*.m4a
*.ogg
!app/tests/e2e/fixtures/silent.mp3
!docs/media/**

# Local Piper install for tools/generate-word-audio.mjs (tools/piper/README.md): a venv and
# a 63MB voice model, both reproducible from that README in a minute
tools/piper/.venv/
tools/piper/models/

# raw takes from the recording studio — megabytes each, and not what ships; the app only
# ever gets the mp3s split out of them
recordings/*
!recordings/README.md
app/tests/e2e/fixtures/generated/

# Azure Functions local env (secrets) — never committed
api/local.settings.json
