# DuoLexie — Todo

Voortgang per fase uit [plan.md](plan.md). Bijwerken bij elke werksessie.

## Phase 0 — Walking skeleton
- [x] Git repo + Vite/React/TS scaffold in `app/`
- [x] Curriculum: `shared/curriculum/sounds.json` (45 klanken, 6 categorieën, verwarringsparen)
- [x] `staticwebapp.config.json` + GitHub Actions workflow
- [x] GitHub repo aangemaakt (`ArjanAssink/DuoLexie`)
- [x] Push naar GitHub
- [x] Azure Static Web App aangemaakt — live op https://jolly-wave-019071410.7.azurestaticapps.net
- [x] Dubbele workflow opgeruimd (Azure-gegenereerde behouden)
- [x] CNAME `duolexie.assink.io` → jolly-wave-019071410.7.azurestaticapps.net
- [x] Custom domain live: **https://duolexie.assink.io** (SSL automatisch)
- [x] Regio-besluit: SWA blijft staan (EUAP-resourcegroep alleen metadata-eigenaardigheid; Cosmos staat in West Europe)

## Phase 1 — Eerste speelbare versie (lokaal, geen backend)
- [x] Padscherm: fases → units → lessen, lineaire unlock, edelstenen + weekdoel (5 van 7)
- [x] **Tijdrit** (heette Flitsen, hernoemd): 60s-rondes, Goed/Nog even, klanken-per-minuut records, NIEUW RECORD-viering
- [x] ~~Klankenjacht~~ verwijderd (niet leuk genoeg bevonden) → vervangen door **Flitsen** (nieuwe naam, was Klankkaarten), geport vanuit [CardFlash](https://github.com/ArjanAssink/CardFlash): tik de stapel om, kaart flipt en vliegt naar de aflegstapel, timer, geen score, geen narratie — geverifieerd in browser + e2e-suite (incl. snel overlappend tikken, geen dubbele afronding); **ronde staat nu vast op 20 kaarten** (`FLITS_DECK_SIZE`) in plaats van de hele klankenpool — die loopt van 5 (de eerste unit) tot 45, dus het spel was eerst vijf tikken lang en aan het eind een zit; onder de 20 herhaalt de pool zich in hele geschudde rondes (iedere klank even vaak), boven de 20 wordt er gesampled met de nieuwe klanken van die unit er altijd in; iOS-jank-fix onderweg (box-shadow tijdens de flip-animatie is een bekend WebKit-perf-probleem — kon zelf niet op een echte iPhone testen, wachtend op bevestiging)
  - [x] **Kaart vegen naar de andere stapel** (plan: [docs/flitsen-swipe.md](docs/flitsen-swipe.md)): naast tikken kun je de bovenste kaart nu ook oppakken en zelf overbrengen — ze blijft aan je vinger kleven, draait om naarmate ze verder richting de aflegstapel komt (dicht op de stapel, op z'n kant halverwege, open erboven), landt als je hem voorbij het midden loslaat (of wegflikt) en glijdt anders terug op de stapel. Tikken is ongewijzigd; de beslissing komt uit `games/swipe.ts`, dat daarvoor een as-parameter heeft gekregen (`resolveDrag`), Hardop lezen en Weetjes merken er niets van. Onderweg gevonden: de meting van de afstand tussen de stapels liep nooit (de component rendert niets tot het deck er is, en de meet-effect draaide alleen bij mount), dus de tik-vlucht flipte al die tijd op zijn plek tussen de stapels in plaats van naar de aflegstapel te vliegen — gefixt. Geverifieerd met unit- en desktop-e2e-tests plus Chromium-schermafbeeldingen op iPhone 13/iPad Pro 11; op een echte iPad nog niet. Na de merge: een korte trilling (8ms) op het moment dat de kaart in je hand op z'n kant gaat — let wel, `navigator.vibrate` bestaat niet op iOS Safari, dus op haar iPad/iPhone is geen enkele trilling in dit spel voelbaar; dit is voor Android.
- [x] Lokale voortgang in IndexedDB (zustand persist): stats per klank, EWMA, mastery
- [x] Audio met TTS-fallback zolang opnames ontbreken
- [x] Opnamestudio `/opnemen` (dev-only) + `tools/convert-audio.mjs`
- [x] Lettertype-toggle (dyslexievriendelijke spatiëring), NL UI
- [x] Frida-redesign geïmplementeerd (art/design_handoff_leerpad): warm licht thema, coin-pad, statbalk, bottom nav, Frida-mascotte + favicons/manifest
- [x] Probeermenu `/proberen` (niet gelinkt in de navigatie): elke spelmodus direct spelen zonder het pad te doorlopen, plus `?test=true` ontgrendelt alle lessen op het echte pad-scherm zelf
- [x] Onboarding bij het eerste bezoek (`/welkom`, zie [docs/onboarding-welkom.md](docs/onboarding-welkom.md)): welkomscherm met Frida, naam kiezen (mag ook niet), avatar maken, confetti — daarna redirect `/` nooit meer; naam + vlag in de bestaande zustand-stores, hydratie-gate in `App.tsx` zodat een terugkerende speler geen flits van het welkomscherm ziet
- [ ] Klanken inspreken (Fase-1-klanken minimaal) en uploaden naar de privé-opslag (niet committen — [docs/private-audio.md](docs/private-audio.md)) — **opnieuw**, via de nieuwe teleprompter + `split-take.mjs`; de 45 oude klik-per-clip-opnames klonken clacky en zijn op 16-09-2026 uit de repo én de geschiedenis gehaald
- [ ] End-to-end test in browser: les afronden, herladen, voortgang blijft staan
- [ ] Testen op haar eigen tablet/device
- [ ] Mascotte kiezen met dochter (suggestie: Flits de vos)

## Phase 2 — Volledige v1-spellenset + PWA
- [ ] Welke klank? (zie teken → tik het juiste geluid)
- [ ] Woordbouwer (woord bouwen uit klank-tegels, mkm eerst)
- [x] Hardop lezen — swipe-kaart in plaats van tap-knoppen: woord verschijnt, wordt na een korte pauze uitgesproken (TTS-fallback, net als klanken — `/audio/words/{id}.mp3` klaarzet voor als opnames er zijn), sleep rechts (goed, "ding") of links (nog even, scheet-buzz via WebAudio); geen re-queue bij fout (leesbeurt, geen drilloop); elk woord telt mee voor de EWMA van al zijn klanken. Node verschijnt pas per unit zodra er ≥4 woorden met de tot-dan-toe geleerde klanken beschikbaar zijn (`words.ts` → `wordsForPool`). Geverifieerd in browser: volledige les (kok/tas/… ) van kaart tot beloningsscherm, progressiebalk, geen console errors/React-warnings.
- [x] Woordenlijsten per klank — `shared/curriculum/words.json` (was al aanwezig, nu voor het eerst gebruikt) dekt kort/lang/twee/drie/vier
- [x] 115 woorden geïmporteerd uit [Hangman](https://github.com/ArjanAssink/Hangman) via `tools/import-hangman-words.mjs` (greedy klanken-segmentatie, geen echte fonetische analyse) — allemaal `"reviewed": false`, **review nodig (Arjan)**: zie `tools/hangman-import-report.md` (woord + hint + klanken-opsplitsing per rij, plus 12 leenwoorden die niet gesegmenteerd konden worden en dus helemaal niet zijn toegevoegd). Prioriteit voor review: de 6 woorden met een `nk`-klank op een samenstellingsgrens (pannenkoek, woonkamer, boerenkaas, boekenkast) — twijfelachtig of dat fonetisch/didactisch hetzelfde is als een `nk` binnen één woorddeel (sprinkhaan, koninkrijk); ik weet het zelf niet zeker genoeg om dat te bepalen.
- [x] `buildWordExercises` geeft voorkeur aan kortere woorden (langste kandidatenpool = 3× het aantal benodigde oefeningen) — voorkomt dat een beginnersles "kat" en "helikopter" door elkaar aanbiedt nu het woordenboek veel langere samenstellingen bevat
- [x] **Hardop lezen rework — lezen → horen → sorteren** (plan: [docs/hardop-lezen-rework.md](docs/hardop-lezen-rework.md)): ronde van 10 **verschillende** woorden (geen herhalingen; woordpool verbreed door één unit vooruit te kijken op het pad — de openingsunit met alleen klinkers krijgt daardoor terecht nóg geen leesnode, de eerste komt op unit 2 met 17 woorden). Lont op de kaart begint op 10s en wordt korter per Leitner-box (10/7/5/3,5/2,5s). **Laat horen**-knop (of tik op de kaart) onthult het woord eerder; die tijd is het snelheidssignaal dat de box laat stijgen. Pas ná het horen is de kaart te beoordelen: sleep of tik naar de zichtbare stapels **Nog even** / **Goed!**, die meegroeien met mini-kaartjes en een teller. Kaart vliegt in een boog naar de stapel, stapel veert, ding + confetti-pufje bij goed, scheet-buzz + woord opnieuw bij nog even. Frida praat mee per fase, 3 goed op rij geeft een Bliksemsprint, pijltjestoetsen werken voor testen op desktop, alles heeft een reduced-motion-variant. Edelstenen nu per **woord**: 5 voor afmaken + 1 per goed woord + 3 perfect (5…18), dus een ronde die helemaal misging levert nog steeds 5 op. Beloningsscherm toont beide stapels ("7 goed · 3 nog even"), de gemiste woorden als aantikbare chips (tik = opnieuw horen) en telt de edelstenen één voor één op. **Proefronde** op `/proberen`: hele fase 1 als pool, om de interactie meteen met haar te testen zonder dat het niveau al klopt.
  - Onderweg gevonden en gefixt: (1) de deck-hint-pseudo-elementen lagen bovenóp de kaart en slikten elke sleepbeweging op (`::after` is het laatste kind, dus bij gelijke z-index vangt die de pointer-events); (2) TTS zonder `onend` (geen nl-NL-stem op een apparaat) zou de kaart nu permanent onbeoordeelbaar maken nu narratie een poort is — `audio.ts` heeft daarvoor een 6s-backstop gekregen, net als de bestaande clip-timeout, plus `stopSpeech()` bij unmount; (3) het kandidatenvenster voor "kortste woorden eerst" moest van 3× naar 2× de rondegrootte omdat een ronde van 10 anders "katapult" ging uitdelen (echt gebeurd in de browser).
  - [x] **Klif in de woordenlijst gedicht:** 93 woorden van 4 t/m 7 letters toegevoegd (fase 1 gaat van 38 naar **131** leesbare woorden, met oploop 28×3, 53×4, 23×5, 7×6, 10×7 letters). Met de hand gesegmenteerd, niet via de greedy importer, onder vier regels die het toevoeg-script ook echt afdwingt: één lettergreep of gesloten lettergrepen met een sjwa (win-ter), geen klinkerdigrafen en geen ch/ng/nk (elk letterpaar getoetst aan de meerletter-klanken in sounds.json), geen c/q/x/y en geen dubbele consonanten, en **geen open lettergrepen** — zomer/lepel/kalender hebben een lange klank met één letter en horen bij een latere stap (precies de val waar de Hangman-import in trapte). Daarna 8 woorden er weer uit die de regels wél toestonden maar een 9-jarige niet tegenkomt (kortst, strikt, brandt, stampt, stamp, stort, sprak, sprints).
  - **Review nodig (Arjan):** de 93 nieuwe woorden staan op `reviewed: false` — met de hand gesegmenteerd is niet menselijk gecheckt. Kleinere, makkelijkere batch dan de Hangman-import. Het meest discutabele is geen segmentatie maar een woordkeuze: de eerste leesnode heeft maar 15 woorden (klinkers + m·s·k·r·t), dus een ronde van 10 is daar noodgedwongen voor de helft clusterwoorden (storm, sterk, korst, markt, trots). Alternatief zou een kortere ronde op juist die node zijn. Zie §4 + §10 van het plan.
  - Bijkomend gevonden: de openingsunit (alleen klinkers) kreeg door die nieuwe woorden ineens wél een leesnode, want de bijgevulde pool haalde 18 woorden — precies de muur waartegen de bijvul-regel bedoeld was. De regel heeft daarom een tweede voorwaarde gekregen: bijvullen mag alleen als ze al *iets* kan lezen (niet-lege strikte pool). Met de huidige woordenlijst is bijvullen verder helemaal niet meer nodig.
  - [x] Woordmodus in de opnamestudio (`/opnemen`): schakelaar Klanken/Woorden, woorden op **kortste-eerst** (`wordsInRecordingOrder()`; padvolgorde binnen dezelfde lengte), standaard alleen de eerste 20 — allemaal woorden van 3 letters. Opslaan als `{woord}.webm` in `app/public/audio/words`, daarna `node tools/convert-audio.mjs app/public/audio/words`. `vite.config.ts` leest die map bij het starten en injecteert `__RECORDED_WORDS__`, zodat een ronde **eerst opgenomen woorden** pakt (binnen het kortste-woorden-venster) en pas op browser-TTS terugvalt als de opnames op zijn. Let op: die lijst is een momentopname bij het starten van Vite — een clip die je tijdens een dev-sessie opneemt vraagt een herstart.
  - [x] **Veeg omhoog/omlaag i.p.v. links/rechts** (spec: [docs/hardop-lezen-swipe-v2.md](docs/hardop-lezen-swipe-v2.md)) — uit de speeltest: horizontaal vegen werkt niet (geen betekenis voor links/rechts, weinig ruimte, botst met iPhone-randgebaren). Nu: **omhoog = Goed!** naar het vakje naast Frida bovenin, **omlaag = Nog even** in het oefenbakje op kaartbreedte onderin; de luisterknop is een badge op de kaart geworden (die ruimte had het bakje nodig) en de stempels staan boven en onder het woord, ín de kaart, zodat ze meevliegen. De beslissing zelf zit in `games/swipe.ts` (80px óf een flick van 0,6 px/ms vanaf 24px, met een asvergrendeling die een scheve veeg laat terugveren) en is los getest zonder DOM. Drie lagen leren haar het gebaar: de kaart veegt zichzelf voor na 1,2s stilte, chevrons wijzen omhoog en omlaag, en een **tik op een stapel voert de veeg zichtbaar uit** (beide richtingen, ~1,4s met een vingerstip erop) — alle drie gaan uit zodra ze **vijf keer zelf** geveegd heeft (`settings.selfSwipes`; tikken en pijltjestoetsen tellen bewust niet mee, anders stopt de uitleg voordat ze ooit geveegd heeft). Pijltjestoetsen zijn nu ↑/↓, en alles heeft weer een reduced-motion-variant.
  - [x] **Opnamepijplijn v2 — één doorlopende opname, automatisch geknipt** ([docs/recording-pipeline-v2.md](docs/recording-pipeline-v2.md)). De klank-opnames klonken "clacky" om vier redenen tegelijk, en alle vier zijn aangepakt: muis/toetsklikken zitten er niet meer in (de take is hands-free, spatie/backspace/Esc zijn het enige en niets daarvan is verplicht), Chrome's AGC + noise suppression + echo cancellation staan uit, knippen gebeurt met 60ms kop en 150ms staart plus fades in plaats van op de stiltedrempel, en `loudnorm` draait één keer over de hele take in plaats van per clip van 300ms.
    - **Studio** (`/opnemen`, dev-only): setup-scherm met set, tempo, microfoonkeuze, live niveaumeter en "test 3 seconden"; daarna een teleprompter die één woord per tempo toont terwijl één `MediaRecorder` loopt. Schrijft `recordings/<kind>-<datum-tijd>.webm` + `.json` (cue sheet). De aftelpiepjes zitten expres in de opname: `split-take.mjs` vindt ze terug en meet daarmee het gat tussen `MediaRecorder.start()` en de eerste sample, in plaats van erop te vertrouwen.
    - **Knippen**: `node tools/split-take.mjs recordings/<take>.webm` → mp3 per woord + `<take>.report.json`. Exitcode 0 alleen als álles `ok` is.
    - **Terugluisteren**: *Rapport laden* op `/opnemen` zet gemarkeerde clips bovenaan, speelt de hele set achter elkaar af, en één knop start een nieuwe take met alleen wat je aanvinkt.
    - `--verify` (whisper.cpp) is er als markeer-pad; de labelmodus voor een take zónder cue sheet is bewust niet gebouwd.
    - Geverifieerd zonder microfoon: 40 unit/tool-tests, een synthetische take die met ffmpeg wordt gegenereerd en weer teruggeknipt, drie studio-e2e-tests, plus één echte browser-take (Chromium met een wav als nep-microfoon) die het hele pad door `split-take.mjs` haalt — 4/4 ok, piepjes gevonden, uitlijning 66ms.
  - **Nog te doen door Arjan:** de 20 woorden daadwerkelijk inspreken via de nieuwe teleprompter (eigen machine, Chrome, stille kamer) + de mp3's committen, en het echte speeltest-moment met haar (§9 stap 5 van het plan). Daarna pas is te horen of de vier fixes hierboven ook echt klinken.
- [ ] **Review nodig (Arjan):** stem/backend kiezen voor bulk woord-audio — zie vergelijking https://claude.ai/code/artifact/12e0af9c-368a-4d27-954e-5ee143df7edc (Piper vs Google Translate vs Azure/Google Cloud), script in `tools/generate-word-audio.mjs`; tot dan speelt Hardop lezen via browser-TTS
- [ ] **Onboarding — eerste bezoek** (plan: [docs/onboarding-welkom.md](docs/onboarding-welkom.md)): wie voor het eerst op de URL landt komt op `/#/welkom` — drie stappen (Frida stelt zich voor + voor wie de app is en wat je kunt verwachten, naam invullen, avatar samenstellen) en een korte "Klaar!"-beat, daarna het pad. Alleen `/` stuurt door (op `settings.onboardedAt === null`, `?test=true` slaat het over); deeplinks nooit. Naam en afgerond-moment blijven lokaal in `progress.ts` (`settings.playerName`, `settings.onboardedAt`) tot de database er is; een hydration-gate wacht op beide stores zodat een terugkerende speler geen flits van het welkomstscherm ziet. Avatar-pickers worden uit AvatarScreen gehaald zodat welkom en profiel dezelfde gebruiken; AvatarScreen krijgt een "Over DuoLexie"-blok met naamveld, work-in-progress-noot, GitHub-issues-link en "Introductie opnieuw bekijken". Meta/OG-beschrijving + manifest-description voor wie de link deelt. E2e: `skipOnboarding`-fixture voor de bestaande specs die op `/` starten, plus een eigen `onboarding.spec.ts`.
- [x] **Beloningsscherm v2 — de viering na een ronde** (plan: [docs/reward-celebration.md](docs/reward-celebration.md)): naar het voorbeeld van Duolingo's "Perfect lesson!"-sequentie. Eerst een hero-beat (Frida 2,2× groot over een schuine streep, gouden kop, gouden confetti), dan zakt alles in de eindopstelling, daarna popt één statkaart in met een voortgangsbalk die tot het goed-percentage vult terwijl het getal meetelt en het label omhoog schakelt (Geoefend → Goed → Super → Perfect!), dan de edelstenen/XP-strip (bestaande optelling blijft) en de nog-even-chips, en pas dan Verder. Eén tik slaat alles over; reduced motion begint meteen in de eindstand. Geen nieuwe tekeningen (`frida-happy.svg` is de hero), drie nieuwe WebAudio-geluidjes (whoosh, cardPop, tierUp), voor álle spellen. Tweede beat na Verder bewust nog niet. **Gebouwd (as built):** `data-beat` op de root stuurt de hele choreografie vanuit CSS, `useCelebration` houdt álle timers vast en `rewardTimeline.ts` is de pure laag (beats, tiers, koppen, confetti, easing — met unittests); balk en percentage komen uit één ge-easede rAF-waarde zodat ze nooit uiteen kunnen lopen, één tik zet alles direct in de eindstand (geen enkele animatie blijft halverwege hangen) en reduced motion start meteen in `done`. Korte schermen (iPhone SE) krijgen een strakker ritme, want met vier chips liep het 47px over. Het probeermenu heeft nu knoppen naar `/#/beloning?goed=…&totaal=…` — het scherm zonder ronde, per tier, waar ook de e2e-tests op draaien (achtmaal een volledige ronde spelen verdubbelde CI). Zie [docs/reward-celebration.md](docs/reward-celebration.md) §13 voor alle afwijkingen; de drie nieuwe geluidjes zijn alleen via de WebAudio-spy geteld, nog niet beluisterd. Na de merge: een lange trilling (~450ms, drie oplopende pulsen en een rommel) op de hero-beat, een enkele zachte bij een stille ronde.
- [x] **Trillen op de iPhone** ([docs/haptics.md](docs/haptics.md)): iOS Safari kent `navigator.vibrate` niet, dus alle trillingen in de app waren daar stil. `audio/haptics.ts` speelt een patroon nu via de schakelaartruc — een verborgen `<input type="checkbox" switch>` (Safari 17.4+) waarvan het label vanuit script wordt aangeklikt geeft het lichte systeemtikje — één tik per korte puls, een ratel van tikjes over een lange (elke 60ms, max 10). Een iPad heeft geen trilmotor en voelt hoe dan ook niets. **Nog te doen door Arjan:** op haar iPhone `/#/proberen` → *Trillen* openen en kijken of de regel *via de iOS-schakelaartruc* zegt en de drie knoppen voelbaar zijn; het is een truc, geen API, en één bron zegt dat Apple hem in iOS 26.5 dichtgezet heeft. Native (Capacitor) is de echte oplossing en staat geparkeerd.
- [x] **Weetjes — dyslexie-feitjes als spel, voor acceptatie** (plan: [docs/weetjes.md](docs/weetjes.md)): per unit één Weetje-node na het Lezen-node, twee kaartjes per keer. Elk kaartje: **Luister** (één zin, wordt automatisch voorgelezen, 🔊 herhaalt) → **Doe** (mythe wegvegen met de bekende veeg uit `games/swipe.ts`, ongewijzigd: omhoog = Waar, omlaag = Niet waar; óf drie keuzes van minstens 56px tikken; óf "Wie is het?" met drie namen, zonder foto's — een gekleurde cirkel met de beginletter in plaats van een portret) → **Bewaar** (de waarheid, altijd positief, bij een misser `pop` + "Goed geprobeerd!" en nooit `bad`/`fart`; kaartje vliegt in het Weetjesboek). Vaste 8 edelstenen en 10 XP per node (`WEETJE_GEMS`), nooit gescoord — het beloningsscherm laat bij `kind: 'weetje'` de score wég en zegt "Nu weet je dit ook! / Vertel het vanavond aan iemand thuis." Weetjesboek op `/#/weetjes` vanuit het profiel: wat ze heeft in kleur, de rest omgekeerd met een `?`, tikken opent het kaartje en leest het terug. `settings.autoRead` (standaard aan) staat in het Over DuoLexie-blok.
  - **Als gebouwd:** 20 uitgeschreven kaartjes + 2 Nederlandse placeholders in `shared/curriculum/weetjes.json`, met bewijsniveau en bron per kaartje; alleen `reviewed: true` wordt ooit uitgedeeld. Narratie eerst de opname (`/audio/weetjes/<id>-fact|doe|reveal.mp3`), anders `utter(…, 0.9)` met de 6s-backstop uit `audio.ts`; nooit twee tegelijk (`stopNarration()` + een volgnummer), en stoppen zet zowel de clip als de synthese synchroon stil. Opnamestudio heeft er een **Weetjes**-set bij (drie cues per kaartje, de zin op de teleprompter, `split-take.mjs` schrijft naar `app/public/audio/weetjes/`). Persist-versie naar **4** met migratie `collectedWeetjes: []`.
  - **Afwijkingen van het plan** staan in `docs/weetjes.md` gemarkeerd met *(as built)*: de woordlimiet voor `fact` is 18 in plaats van 12 en een `reveal` mag 3 zinnen zijn (§4's copy is definitief en paste niet in §3's getallen), "Goed geprobeerd!" is een eigen element en een eigen zin in plaats van een prefix op `reveal` (de opname van een reveal is voor beide uitkomsten dezelfde), het boekje in de spelbalk is tijdens een ronde geen link (dan zou ze de node halverwege weggooien), en in het Weetjesboek bladeren gaat met pijlen, niet met een veeg.
  - **Review nodig (Arjan) — 8 kaartjes staan op `reviewed: false` en worden dus niet uitgedeeld:** `piraten` (Keira Knightley — dat ze *hardop* met filmscripts oefende staat in geen van de bronnen die ik open kreeg; dat ze met het script van *Sense and Sensibility* leerde lezen wél), `regisseur` (Steven Spielberg — "op zijn 60e" spreken de bronnen elkaar tegen; AdditudeMag komt op ~65), `percy` (geen enkele bron dekt én "alle halfgoden hebben dyslexie" én "de schrijver bedacht het voor zijn zoon"; het eerste staat in het boek zelf, het tweede in Riordans interview), `luisteren` (Stichting Lezen vindt wél winst voor woordenschat maar juist verlies voor verhaalbegrip — de zin zegt "woorden en verhalen"), `vinger` (geen bron gevonden die de vinger-onder-de-regel-truc echt onderbouwt; de BDA noemt alleen leeslinialen), `grote-geheel` (bewust `ervaring`: de "dyslexie-voordeel"-literatuur is gemengd, vandaar de verplichte "vertellen dat"-formulering — bron is Taylor & Vestergaard, die het zelf ook voorzichtig brengt), en `nl-1` + `nl-2` (de twee Nederlandse namen, leeg — jij kiest ze, eventueel met de RID-behandelaar).
  - **Wél nagekeken** (bron geopend en klopt met de claim): `niet-alleen`, `familie`, `ogen`, `oefenen`, `extra-tijd`, `voorlezen` en `grieks` uit de spreekbeurt-pdf van Dyslexie Centraal; `slim` uit "Wat is dyslexie?" van Dyslexie Centraal ("Dyslexie staat los van intelligentie, het heeft dus niks te maken met hoe slim je bent"); `elke-taal` uit BBC Science Focus; `ruimte` uit Zorzi e.a. 2012 (PNAS) via ScienceDaily; `spiderman` (NBC, Tom Holland zelf), `kok` (HuffPost, Jamie Oliver zelf — de "wel twintig boeken" is daarnaast tegen Wikipedia's bibliografie gehouden, dat zijn er 37), `legolas` (Child Mind Institute, Orlando Bloom zelf) en `lettertype` (dyslexiefont.com over Christian Boer).
  - **Open vraag uit §11, ongewijzigd:** heeft Frida zelf dyslexie? En: moet "Vertel het vanavond aan iemand thuis" ook in het Weetjesboek staan?
- [ ] **Opnamestudio v3 — schone clips, en bijhouden wat goed is** (plan: [docs/recording-studio-v3.md](docs/recording-studio-v3.md)). De v2-pijplijn heeft nog geen clip opgeleverd die in de app zit; dit plan gaat over de eerste échte take. **Tier 1**: elke cel in het overzicht is een afspeelknop (+ "Alles afspelen", toetsen ←/→/Enter), elke clip krijgt een oordeel ✅ goed / ❌ afgekeurd (G/A tijdens afspelen), afkeuren telt als ontbrekend zodat de volgende take hem vanzelf opnieuw doet en het oordeel reset zodra een nieuwer bestand verschijnt; niveaumeter + clip-teller in de teleprompter, doelzone op de setup-meter, stiltemeting (ruis/brom), highpass 70 Hz, retake-luidheid gelijk aan de originele take, checklist, twee bugs (Weetjes-rapport speelt uit de verkeerde map; ids i.p.v. zinnen). **Tier 2**: Vite-dev-plugin `/__studio/` — take uploaden, knippen en rapport laden met één knop, geen terminal en geen herstart meer (virtuele module i.p.v. `__RECORDED_WORDS__`). **Tier 3** (tweede PR): golfvorm met cue-vensters, auto-tempo, kalibratiezin + drift-check, oud/nieuw vergelijken, luisteren op de iPad, WAV-opname, externe recorder gedocumenteerd, klank-hints.
  - [x] **Tier 1 gebouwd** (Claude Opus 5, 1M context). Elke cel in het overzicht is een afspeelknop met cache-busting op de `Last-Modified` van het bestand; ←/→/Enter lopen het raster af; **Alles afspelen** / **Alleen onbeoordeeld** met 400 ms ertussen, en **G**/**A** beoordelen wat er klinkt en springen meteen door. Afkeuren telt als ontbrekend, dus de volgende take pakt het woord vanzelf; het oordeel hangt aan het *bestand*, dus een retake erft nooit het ❌ dat hem terugstuurde. Niveaumeter met peak-hold + clip-teller in de teleprompter (een overstuurd woord markeert zichzelf voor een retake, één keer per woord), doelzone en oordeel-in-woorden op de setup-meter, stiltemeting met ruisvloer + brom-detectie op 50/100 Hz, `highpass=f=70:poles=2` in de decode-stap, retake-luidheid overgenomen van de take die hij vervangt, checklist, en beide §2.8-bugs (Weetjes-rapport speelde uit `/audio/words/`; rijen en cellen tonen nu de zin met de id eronder).
  - [x] **Tier 2 gebouwd**. `app/vite-plugins/studio.ts` serveert `/__studio/` (take uploaden, knippen met live voortgang, rapport, takes-lijst, verdicts, afkeuren-naar-`recordings/afgekeurd/`). Eén knop **Knip en beluister**, geen commando en geen herstart meer: `__RECORDED_WORDS__` is vervangen door een virtuele module waarvan de plugin de Sets ter plekke bijvult zodra er een mp3 verschijnt. `npm run take:split` blijft voor wie liever de terminal pakt. De productie-build bevat geen `/__studio`-code (gecontroleerd met grep op `dist/`).
  - [x] **De twee naden voor privé-audio** (§0 van de sequencing): `app/src/audio/recorded.ts` is het enige antwoord op *heeft deze id een clip* én de enige plek waar een clip-URL wordt gebouwd (`clipSrc(kind, id, versie)`). `words.ts`, `weetjes.ts`, `audio.ts`, het beoordeelraster, het rapport én de HEAD-probe van de studio lopen erdoorheen; nergens staat nog `/audio/<map>/<id>.mp3` los in de code.
  - **Afwijkingen van de spec** staan in [docs/recording-studio-v3.md](docs/recording-studio-v3.md) gemarkeerd als *(as built)*. De grootste: de gevraagde 20 dB demping op 50 Hz is met `highpass=f=70:poles=2` niet haalbaar (gemeten 6,8 dB; 20 dB ligt op 20 Hz), en steiler filteren zou ~3 dB kosten op 100 Hz — de grondtoon van een stem. En "korter dan 30 s spraak" is gelezen als 30 s *take*, omdat spraak ongeveer een kwart van een leestake is en de letterlijke lezing élke take inclusief de eerste op het hergebruikpad zou zetten.
  - **Nog te doen door Arjan (§6):** de echte take — checklist, stiltemeting, 20 startwoorden, *Knip en beluister*, alle twintig beoordelen met G/A, de ❌-set opnieuw, luisteren op de iPad, mp3's committen. Pas dan is te horen of het ook echt schoner klinkt; niets in deze PR is met oren gecontroleerd.
  - **Tier 3** (tweede PR): golfvorm met cue-vensters, auto-tempo, kalibratiezin + drift-check, oud/nieuw vergelijken, luisteren op de iPad, WAV-opname, externe recorder gedocumenteerd, klank-hints.
- [ ] **Privé audio — stemopnames uit de repo en van de openbare site** (plan: [docs/private-audio.md](docs/private-audio.md)). Clips naar een privé Blob-container, uitgeleverd via `/api/audio/{kind}/{id}` met een HMAC-token van 15 minuten, 60 verzoeken/minuut per IP, Range-support (Safari), `Cache-Control: private` en `noindex`; manifest via de API i.p.v. `__RECORDED_*__` in de bundel; de app prefetcht de tien woorden van een ronde bij de start zodat de cold start van Functions vóór de eerste kaart valt. Lokaal: dev-middleware die dezelfde drie routes uit `recordings/clips/` serveert, splitter schrijft daarheen, `tools/upload-audio.mjs` naar Azure. CI-guard tegen audio in de tree én in de branch-geschiedenis. README-blok + licentienoot voor wie de app forkt. **Gedaan (16-09-2026):** de 45 klank-mp3's uit de werkboom gehaald en `app/public/audio/` gitignored, dus ze staan niet meer op de live site. **Volgorde (§0 van het plan):** eerst opnamestudio v3 tier 1+2 mergen, dán de geschiedenis herschrijven, dán dit plan bouwen — een branch van vóór de herschrijving mag er niet ná gemerged worden. **Nog te doen door Arjan:** de junk-branch `claude/onboarding-welkom-shots` verwijderen (19.716 bestanden incl. een tweede kopie van de opnames in `app/dist/`), daarna `git filter-repo` (§9) en GitHub-support vragen de gecachte commits op te ruimen; storage account + container + twee app settings (§3).
- [ ] Eindbaas + Schatkist nodes op het pad
- [ ] vite-plugin-pwa: installeerbaar, offline audio-precache

## Phase 3 — Accounts & sync
- [x] Cosmos DB account aangemaakt (West Europe; check: free tier discount "Applied")
- [x] Database `duolexie` + containers `auth`/`data`
- [x] Env vars op SWA (`COSMOS_ENDPOINT`, `COSMOS_KEY`, `JWT_SECRET`) — geverifieerd via `/api/health`
- [x] `api/` package opgezet met `/api/health` canary (env + Cosmos-check)
- [ ] `api/` package: register/login (bcryptjs, JWT-cookie), profiles + PIN, progress sync
- [ ] Registratie/login + profielkiezer met avatars
- [ ] Outbox-sync (idempotent op sessionResult-id); lokale voortgang migreren naar profiel

## Phase 4 — v2 content & ouderdashboard
- [ ] Fases 4–7 content (tweelingklanken, ch/ng/nk, drie/vier tekens, woordenrijk)
- [ ] Klankzoeker + Woordenvangst (ei/ij, au/ou spelling)
- [ ] Ouderdashboard: heatmap per klank, actieve-klanken-configuratie
- [ ] Stickerboek

### Speler-avatar & winkel (zie plan.md §12)
- [x] `AvatarView`: layered SVG rig (body/head/eyes/hair), `crop` prop (full / topbar), placeholder art — geverifieerd in browser (Playwright): kleuren + kapsel wisselen live, persisteert over reload (IndexedDB), geen console errors
- [x] `state/avatar.ts`: avatarConfig, persisted zoals progress.ts (ownedItems volgt bij de shop-stap)
- [x] Topbar: `Frida` in `.statbar` vervangen door `<AvatarView crop="topbar">`, tikbaar naar AvatarScreen; bottomnav "Profiel" navigeert ook naar /avatar
- [x] `AvatarScreen`: volledige bovenlijf-weergave, kleurkeuzes (huid/ogen/haar), kapsel-picker (kort/krullen/staart/lang)
- [x] Accessoire-slots (oorbellen, bril, hoed) in de rig + `AvatarConfig.equipped` — geverifieerd: 3 items tegelijk dragen (bril + pet + oorbellen) rendert correct gestapeld
- [x] Shop: `shopItems.json` catalogus (7 items), `spendGems` op progress.ts, `ShopScreen` per categorie — geverifieerd: kopen trekt gems af, niet-betaalbare items zijn uitgeschakeld en doen niets bij een tik, gedragen/eigendom-status en persistente over reload kloppen, geen console errors
- [x] Echte kunst-pas: placeholder-vormen vervangen door verfijnde hand-getekende SVG (rechtstreeks in code, niet via de design-canvas — lagen moeten pixelperfect uitlijnen tussen huid/oog/haarkleuren, kapsels en accessoires; vlakke shading i.p.v. gradients omdat meerdere AvatarViews tegelijk renderen). Geverifieerd in browser: 4 kapsels, meerdere kleurcombinaties, alle 7 accessoires los en gestapeld, ook op winkel-thumbnail-schaal — geen console errors

## Later
- [ ] Fase 8 zinnen: Verdwijnzinnen (RAP-stijl), Zinnenbouwer
- [ ] Spraakherkenning stil meescoren naast zelfbeoordeling
- [ ] Vraag RID-behandelaar: klankgebaren? huidige actieve klanken?
